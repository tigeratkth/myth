---
description: 每次会话必读协作复盘笔记，按事件触发更新
alwaysApply: true
---

# 协作复盘工作流

本工作空间维护一份 `RETROSPECTIVE.md`（位于工作区根目录），用于累积用户与 AI 协作过程中的经验教训。

## 你必须做的事

1. **会话开始时**：主动读取 `RETROSPECTIVE.md`，理解已有经验，避免重复犯同样的错误。
2. **触发式更新**（必须立即追加条目，不要等到会话结束）：
   - 用户明确纠正、否定或表达不满 → 追加"纠正记录"
   - 完成一个阶段性任务 → 追加"阶段复盘"
   - 发现自己踩坑（工具误用、理解偏差等）→ 追加"教训记录"
3. **提炼通用经验**：当同类教训出现 ≥ 2 次，将其抽象到文件顶部的"通用经验"区块。
4. **写入格式**：严格遵循文件内部定义的模板；保持条目精简（3-8 行）；使用中文。

## 你不要做的事

- 不要记流水账或复述显而易见的事实。
- 不要未经用户确认就删除/重写已有条目（可以新增条目来"纠偏"旧条目）。
- 不要把这个文件当成 TODO 列表；它只记录经验。

## 运行边界说明

我（AI）无法按真实时间定时触发。所谓"每10分钟复盘"在实际执行中等价于：
**"每次会话开启时读一次 + 每次被用户纠正或完成阶段性工作时立即写一次"**。
